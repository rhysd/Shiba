'use strict';

// Workaround for 'electron' module

const EventEmitter = require('events').EventEmitter;

class App {
    addRecentDocument(file) {
        // Do nothing
    }
    getPath(kind) {
        return '';
    }
}

class IpcMain extends EventEmitter {
    constructor() {
        super();
        this.setMaxListeners(Infinity);
    }
}

exports.app = new App();
exports.ipcMain = new IpcMain();
